# Fine-grained GitHub PAT, no scopes/permissions needed (public data only).
# Used server-side by lib/github.ts for the contribution graph. Optional —
# the section is hidden when unset.
GITHUB_TOKEN=
